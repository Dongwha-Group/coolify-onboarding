{
  "presets": [
    {
      "id": "nextjs",
      "label": "Next.js 단독 (Frontend)",
      "techStack": "Next.js, TypeScript, Tailwind CSS",
      "devCommand": "npm run dev",
      "buildCommand": "npm run build"
    },
    {
      "id": "fastapi",
      "label": "FastAPI 단독 (Backend)",
      "techStack": "FastAPI, Python, Poetry, Pydantic",
      "devCommand": "poetry run uvicorn app.main:app --reload",
      "buildCommand": "docker build -t api ."
    },
    {
      "id": "fullstack",
      "label": "Next.js + FastAPI 모노레포",
      "techStack": "Next.js, FastAPI, Poetry, Docker Compose",
      "devCommand": "docker compose up --build",
      "buildCommand": "docker compose build"
    }
  ]
}
